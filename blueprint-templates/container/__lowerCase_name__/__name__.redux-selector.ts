import {Selector} from 'react-redux';
import {RootStoreState} from '@shared-state';
import { {{name}}ReduxSelectionState} from './types';

export const {{name}}Selector: Selector<RootStoreState, {{name}}ReduxSelectionState> = (
  state,
) => {
  return {};
};
