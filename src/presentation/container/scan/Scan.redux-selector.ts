import {Selector} from 'react-redux';
import {RootStoreState} from '@shared-state';
import {ScanReduxSelectionState} from './types';

export const ScanSelector: Selector<RootStoreState, ScanReduxSelectionState> = (
  state,
) => {
  return {};
};
